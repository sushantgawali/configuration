---

- name: Configure instance(s)
  hosts: all
  sudo: True
  gather_facts: True
  roles:
    - { role: 'docker', runit_service: 'mongodb', stop_services: true }
    - { role: 'mongo', mongo_create_users: true }

