---

- name: Configure instance(s)
  hosts: all
  sudo: True
  gather_facts: True
  roles:
    - { role: 'docker', runit_service: 'mysql' }
    - { role: 'docker', runit_service: 'memcached' }
    - edxlocal
